{
  "rules": {
    "settings": {
      ".read": true,
      ".write": "auth != null && auth.token.email === 'hussainalhashem99@gmail.com' && auth.token.email_verified === true"
    },
    "submissions_public": {
      ".read": true,
      "$id": {
        ".write": "!data.exists() || (auth != null && auth.token.email === 'hussainalhashem99@gmail.com' && auth.token.email_verified === true)"
      }
    },
    "submissions_private": {
      ".read": "auth != null && auth.token.email === 'hussainalhashem99@gmail.com' && auth.token.email_verified === true",
      "$id": {
        ".write": "!data.exists() || (auth != null && auth.token.email === 'hussainalhashem99@gmail.com' && auth.token.email_verified === true)"
      }
    }
  }
}